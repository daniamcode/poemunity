one the nextjs thing is done and the production_checklist.md is achievement, we want to:
- See how to do a distribution to assign likes to and from diferent users, assgining more likes to famous poems, and to tematics related to the personality of each user etc.
- See if we maintain disqus (price?) or we do our own solution or another provider
- AI community simulation plan exists at `docs/AI_COMMUNITY_SIMULATION.md`. First one-shot pre run is done (`seed-activity-v1` + `seed-activity-v1.1-likes`). Rollback script exists at `scripts/simulation/rollback-run.mjs` and was dry-run verified against pre. Next work: human QA pre, verify production dashboard/backup items, then seed production only after the checklist is cleared.
- I hed to check the Monarch idea to see 1) What part is script and what part is ai generated and 2) how to call claude as Monarch calls copilot, invisibly
- Enable GitHub branch protection so required checks must pass before merge. Vercel now also runs repo-level build gates for frontend and backend deploys.
- Is it normal to have public backend? https://poemunity-backend.vercel.app/
- Enforce Applitools visual diffs in CI: switch `eyes.closeAsync()` → `eyes.close()` in `frontend/selenium/visual.spec.ts` once the known visual issues are accepted as baseline in the Applitools dashboard. Currently diffs are recorded but do not fail the test run.
- Add E2E test for the full registration flow: fill in username, email and password, submit, and verify the user lands on the correct page and can log in. Cover the error cases too (duplicate username, duplicate email, invalid email format).
- Responsive typography: large display sizes (`$fs-3xl` 40px on Authors page title, `$fs-4xl` 45px on author detail name, `$fs-display` 35px on Login/Register headings) are not scaled down for mobile. Evaluate whether these need responsive overrides at smaller breakpoints (`$bp-sm`/`$bp-md`).
- Manually verify all toast notifications work end-to-end in the browser: comment post, reply, delete; poem like failure; poem delete; poem create/save. Especially check that error toasts from Redux actions (commonActions.ts) show a readable message and not [object Object].
- AppContext re-render optimisation (post-launch, not urgent — re-renders only fire on login/logout/profile update). Quick win: pass individual fields (`user`, `userId`, `isAdmin`, `config`) to `ListItem` instead of the whole `context` object so `React.memo` comparison is field-level. Full fix: split `AppContext` into `AuthContext` (auth/user fields) + a separate context for the rest, so consumers only re-render when the fields they actually use change. Also: `elementToEdit` is in the `Context` interface but never read from context — `useProfileForm.ts` already derives it from URL query params (`router.query.edit`), so it can be removed from the interface during this refactor.
