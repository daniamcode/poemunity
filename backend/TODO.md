one the nextjs thing is done and the production_checklist.md is achievement, we want to:
- See how to do a distribution to assign likes to and from diferent users, assgining more likes to famous poems, and to tematics related to the personality of each user etc.
- See if we maintain disqus (price?) or we do our own solution or another provider
- Crete a plan (in a md file) to create some kind of cron, where we call, let's say 2 times a day on random hours, that cron that reviews new comments. Then, if comments are done in poems, tell an agent (deterministically via script?) to tell users with those interests to add comment (not always) there. If the message is direct to the user (to its profile, add there a comments section) then that specific user should answer (the answers ned to mimic human behaviour, and the personality of the user, so sometimes answering with a question, sometimes not, etc). That plan to consider also some periodic interaction started by ai users too, into particular poems but aslo directly in the profile of certain users (again, based on the interests ot the ai user). AI agents have to create poems also in a programatic way, with some kind of time rule, genre disgribution etc. So basically i need to mimic a whole human community with common human interactions and behaviours. I also need to do likes and unlikes for ai users with a certain distribution and periodicity
- I hed to check the Monarch idea to see 1) What part is script and what part is ai generated and 2) how to call claude as Monarch calls copilot, invisibly
- Fix github actions in github, they are broken. Relate that somehow with vercel deployment to block it if that pipeline breakes
- Is it normal to have public backend? https://poemunity-backend.vercel.app/
- Auth refactor: remove localStorage from the auth flow and rely solely on the HttpOnly `token` cookie. Currently the app uses a hybrid — cookie for middleware route protection, localStorage for client-side AppContext (read by Header.tsx useEffect). The right approach: drop the localStorage write on login, ensure all pages pass `initialUser` via `getServerSideProps`, and derive user state from SSR + cookie only. This removes XSS exposure and eliminates the two-sources-of-truth problem.
