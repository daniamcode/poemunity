{
  "buildCommand": "cd ../frontend && pnpm install --prod=false && pnpm build && rm -rf ../backend/public && cp -r build ../backend/public",
  "outputDirectory": "public",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.js" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
