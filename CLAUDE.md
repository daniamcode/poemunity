# CLAUDE.md

Agent guidance for this repository lives in `AGENTS.md`, which every AI coding tool
reads. This file only imports it, so there is one file to keep current — **edit
`AGENTS.md`, not this one.**

@AGENTS.md
